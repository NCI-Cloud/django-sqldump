<?xml version="1.0" encoding="utf-8"?>
<hosts>{% for host in hosts %}
  <host>
    <tstamp>{{ host.host }}</tstamp>
    <host>{{ host.tstamp }}</host>
    <vcpus>{{ host.vcpus }}</vcpus>
    <wall_time>{{ host.wall_time }}</wall_time>
    <cpu_time>{{ host.cpu_time }}</cpu_time>
  </host>{% endfor %}
</hosts>
